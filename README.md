# azimut-medline-site

Статический сайт центра ментального здоровья "Азимут Медлайн".

## Что внутри

- `index.html` - главная страница.
- `about.html`, `services.html`, `prices.html`, `doctors.html`, `blog.html`, `contacts.html`, `partnership.html`, `payment.html` - основные разделы.
- `article.html` - шаблон страницы статьи, наполняется через query-параметр `?slug=...`.
- `privacy.html`, `personal-data.html`, `terms.html` - юридические страницы.
- `css/` - общие стили, адаптив и стили аудиоплеера.
- `js/` - навигация, динамический рендер карточек, формы, блог, аудиоплеер, заготовки интеграций.
- `data/` - контентовые данные: услуги, цены, врачи, отзывы, статьи, аудиотреки.
- `assets/` - логотип, изображения и аудиофайлы.

## Как запустить локально

Проект не требует сборки.

```bash
python3 -m http.server 8000
```

После запуска открыть:

```text
http://localhost:8000/
```

## Текущее состояние

- Ветка `main` синхронизирована с `origin/main`.
- Рабочее дерево было чистым перед обновлением этого README.
- Последний коммит на момент аудита: `7ed7792 Update site files`, 11 июня 2026.
- Проверены локальные ссылки, якоря и подключаемые ресурсы: 13 HTML-страниц, 840 ссылок/ресурсов, битых локальных путей не найдено.
- Все основные страницы отвечают по локальному HTTP-серверу со статусом `200`.

## Что уже собрано

- Главная с формой заявки, блоками услуг, форматов помощи, врачей, статей, отзывов и аудиоплеером.
- Раздел услуг с направлениями: наркология, психиатрия, психология.
- Раздел цен с прайсом и FAQ.
- Раздел врачей с карточками направлений и изображениями.
- Блог и страница статьи на основе данных из `data/articles.js`.
- Контакты, партнёрство, оплата и юридические страницы.
- Формы заявок подготавливают payload для будущей CRM-интеграции.
- Аудиоплеер работает с плейлистом из `data/audio.js`.

## Открытые вопросы перед релизом

- Заменить временные юридические данные: сейчас в футере и документах стоят демонстрационные реквизиты.
- Заменить временный адрес `г. Москва, ул. Примерная, д. 1, офис 1`.
- Уточнить реальные ФИО и специализации врачей вместо "Специалист будет добавлен после согласования".
- Уточнить данные директора клиники в `data/doctors.js`.
- Заменить Telegram-ссылки `https://t.me/` на реальный аккаунт или убрать кнопки.
- Подключить реальную карту на странице контактов вместо `.map-placeholder`.
- Подключить Bitrix24 вместо `js/bitrix24-placeholder.js`.
- Подключить платёжного провайдера вместо `js/payment-placeholder.js`.
- Согласовать финальные цены и медицинские формулировки.

## Быстрые проверки

Проверить статус страниц:

```bash
for page in index.html services.html doctors.html prices.html blog.html article.html contacts.html payment.html privacy.html terms.html personal-data.html partnership.html about.html; do
  code=$(curl -s -o /dev/null -w '%{http_code}' "http://localhost:8000/$page")
  printf '%-20s %s\n' "$page" "$code"
done
```

Проверить git-состояние:

```bash
git status --short --branch
```
